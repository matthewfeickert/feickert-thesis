version: 2
jobs:
  build:
    docker:
      - image: debian:stretch
    steps:
      - checkout
      - run:
          name: Greeting
          command: echo "This is Matthew's Thesis"
      - run:
          name: Print the Current Time
          command: date
